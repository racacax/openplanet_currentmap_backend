version: '3.6'

services:
    web:
        image: currentmap
        build: .
        command: python manage.py runserver
        working_dir: /app
        ports:
            - "8001:8001"
        volumes:
            - .:/app